## Ева-стиль 1.4.2

### Исправлено
- Проверка обновлений больше не падает с HTTP 403 от GitHub API
- Используется публичный `latest.yml` (как у Electron), без лимита API

### Важно
Версию **1.4.1** нужно один раз поставить вручную (с неё автопроверка не дотягивается до исправления). Дальше обновления снова через кнопку / при запуске.

### Файлы релиза
| Файл | Описание |
|---|---|
| `eva-style-setup-1.4.2.exe` | Установщик (рекомендуется) |
| `eva-style-1.4.2-portable.exe` | Портативный запуск без установки |
| `latest.yml` | Для автообновления (Tauri и Electron 1.3.0) |
