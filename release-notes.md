## Ева-стиль 1.2.3

### Исправления
- **Звук запуска и логотип** — в установленной (и автообновлённой) версии снова находятся файлы `startup.mp3` и `icon.png`. В 1.2.2 абсолютные пути `/…` ломались при загрузке через `file://`.

### Файлы релиза
| Файл | Описание |
|---|---|
| `eva-style-setup-1.2.3.exe` | Установщик (рекомендуется, с автообновлением) |
| `eva-style-1.2.3-portable.exe` | Портативная версия (без установки) |
| `latest.yml` | Метаданные для автообновления |
